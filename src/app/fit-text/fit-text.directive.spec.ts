import { FitTextDirective } from './fit-text.directive';

describe('FitTextDirective', () => {
  it('should create an instance', () => {
    let directive = new FitTextDirective();
    expect(directive).toBeTruthy();
  });
});
